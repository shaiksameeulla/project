$ git clone git@github.disney.com:RAMAD016/AccommodationFulfillment.git

Go into that project                                                                                                           

$ git remote add upstream url..(forked repo)
(.ssh url)

$ git remote -v

$ git fetch upstream

$ git pull upstream develop

If u want to remove upstream url--- 

$ git remote remove upstream

checkout branch:

git c